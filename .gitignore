node_modules/
dist/
.DS_Store
*.local
.vite/

# Scratch scripts (screenshot harnesses, audit probes). Never committed.
scripts/_*

# Python — packages/core
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Python — the server virtualenvs. Each server under servers/ installs its own,
# because each is a standalone deployable that must be installable without the
# others (see servers/README.md). They are build output, not source.
servers/*/.venv/
