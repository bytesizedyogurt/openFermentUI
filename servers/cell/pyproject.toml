[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "geneos-mcp"
version = "0.1.0"
description = "geneOS host server over MCP: strains and their records from the real corpus, flux prediction declared and refusing."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.2",
    # The canonical schema. Everything this server serves is validated through
    # openferment_core on read; it hand-writes no entity of its own.
    "openferment-core",
]

[project.scripts]
geneos-mcp = "geneos_mcp.server:main"

[tool.uv.sources]
# Editable — see servers/process/pyproject.toml for why. A container image
# installs non-editably and sets OF_CORPUS_DIR.
openferment-core = { path = "../../packages/core", editable = true }

[tool.hatch.build.targets.wheel]
packages = ["geneos_mcp"]
