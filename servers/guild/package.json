{
  "name": "@openferment/server-guild",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MCP server skeleton for the Guild subsystem: chapters, deposits, seals. Will wrap ORCID and in-toto.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "start": "tsx src/server.ts",
    "smoke": "tsx scripts/client.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
